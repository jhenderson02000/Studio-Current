# Studio Current Deployment

Current public site:

`https://studio-current.vercel.app`

Primary launch path:

`Vercel`

Current production URL:

`https://studio-current.vercel.app`

Important:

- `studiocurrent.com` is already in use by another company as of April 23, 2026.
- `studiocurrentdaw.com` is not attached to this project and should be treated as a future custom-domain option only.
- The working public URL today is `https://studio-current.vercel.app`.

Configured hosts in this repo:

- Vercel via [vercel.json](./vercel.json)
- Netlify via [netlify.toml](./netlify.toml)
- GitHub Pages via [.github/workflows/deploy-pages.yml](./.github/workflows/deploy-pages.yml)

Recommended next steps:

1. Import the repo into Vercel as a static site.
2. Use the production Vercel URL `https://studio-current.vercel.app`.
3. Verify recording, microphone permissions, playlist drag/drop, and playback over HTTPS.
4. If you later buy a domain, attach it to the Vercel project from `Settings` -> `Domains`.
5. Until then, share `https://studio-current.vercel.app`.

Notes:

- The current project is a static web app, so it can be hosted without a backend.
- Browser audio input works best over `https` in production.
